{
  "name": "azure-static-webapp",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --coverage",
    "build": "react-scripts build",
    "lint": "eslint src/",
    "sonar": "sonar-scanner"
  },
  "devDependencies": {
    "sonarqube-scanner": "^3.0.1",
    "jest": "^29.0.0",
    "eslint": "^8.0.0"
  },
  "jest": {
    "collectCoverageFrom": [
      "src/**/*.{js,jsx,ts,tsx}",
      "!src/index.js"
    ],
    "coverageReporters": ["lcov", "text"]
  }
}